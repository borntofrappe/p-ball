export type Entry = {
  no: string;
  name: string;
  category: string;
  height: number;
  weight: number;
  description: string;
  src: string;
};

export type Pokemon = {
  name: string;
  src: string;
};

export type Area = {
  name: string;
  src: string;
};

export type CatchEntry = {
  name: string;
  version: string;
  src: string;
};

export type CatchArea = {
  name: string;
  version: string;
  src: string;
};
