type BaseEntry = {
  name: string;
  uri: string;
};

type Entry = {
  no: string;
  name: string;
  category: string;
  height: number;
  weight: number;
  description: string;
  uri: string;
};

type Area = {
  name: string;
  uri: string;
};

type Item = {
  name: string;
  uri: string;
};

type Version = "Red" | "Blue";
